name: Terraform Run
run-name: ${{ github.event.commits[0].message }} / ${{ github.event.pull_request.commits_url  }}  🚩

on:
  pull_request:
    branches:
      - main
      - dev
      - ft
  workflow_dispatch:
      
env:
  branch: ${{ github.base_ref }}

jobs:
  Terraform:
      name: "Terraform Run"
      runs-on: ubuntu-latest
      steps:
        - name: Checkout
          uses: actions/checkout@v4.1.1
  
        - name: nothing
          run: echo "nothing101"
